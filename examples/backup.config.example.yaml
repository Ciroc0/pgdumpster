projectRef: abcdefghijklmnopqrst

backup:
  output: ./backups
  consistency: verified
  maxStorageConcurrency: 8
  maxApiConcurrency: 3
  maxConsistencyRetries: 3

encryption:
  mode: age
  recipient: age1replace_with_real_recipient
  # Needed when this config is also used to read .tar.zst.age bundles.
  identityFile: ./age-identity.txt

destination:
  type: local

logging:
  level: info
  file: null
