/goal Build pgDumpster to production-ready source-available quality from the specifications in this repository. Treat AGENTS.md, docs/01-product-requirements.md, docs/02-coverage-matrix.md and docs/13-acceptance-criteria.md as binding. Before implementation, read the complete documentation set, revalidate every unstable Supabase Management API/data-plane contract against current official Supabase docs/OpenAPI, and update PLANS.md with milestones and decisions.

Implement the complete product, not a prototype: strict TypeScript ESM CLI; doctor, backup, inspect, verify and restore; full PostgreSQL logical state, including dedicated capture of auth/extension-owned schemas excluded by normal Supabase CLI dumps; migration history, Cron/pg_cron, Queues/pgmq, Database Webhooks, Vault data and managed-schema customizations; Vault/pgsodium root key; Auth config/SSO/TPA/signing-key coverage; API keys; Edge Functions plus secrets; File Storage bytes+metadata; Vector Storage; Analytics/Iceberg wherever exportable; Realtime/PostgREST/Storage/database/network/domain/backup/log-drain/project configuration; capability discovery; coverage statuses; SHA-256 manifest; cross-service drift/consistency detection; resume; bounded concurrency; rate-limit handling; dry-run restore; local and S3-compatible destinations; secure secret handling; cross-platform UX; machine-readable JSON; and explicit platform-limit reporting. Never silently omit state or label a backup complete when an exportable component failed.

Do not fake unsupported Supabase behavior. If Supabase does not expose a source value or cannot recreate the same value, preserve all available metadata, mark it not_exportable or non-identically-restorable as specified, and emit an actionable restore report. Preserve API-key/signing-key/Vault semantics exactly as current Supabase contracts allow.

Create all implementation, schemas, fixtures, docs, examples, CI, security, contributor and release files needed for a professional public source repository. No placeholder production code, TODO adapters, stubbed tests, disabled checks, ignored failures, or secrets in fixtures/logs. Use maintained locked dependencies and runtime-validate source contracts.

The goal is complete only when formatting, lint, strict typecheck, build, unit tests, contract tests, local integration tests, security checks, archive traversal tests, interruption/resume tests, and OS-supported CI pass; live managed-Supabase backup→verify→restore E2E has passed on dedicated source/target test projects; semantic parity is verified for every exportable applicable component; corruption and secret leakage tests pass; README/setup/CLI docs match real behavior; and a final review finds no unresolved high/critical defects. If live credentials are unavailable, complete everything possible locally, leave the live E2E gate explicitly unfulfilled, and do not claim the overall goal is complete.
